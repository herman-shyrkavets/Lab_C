<Window x:Class="GraphFunctionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Лабораторная работа" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" Grid.Row="0">
            <Label Content="X1:"/>
            <TextBox x:Name="txtX1" Width="60" Margin="5 0" Text="-7"/>
            <Label Content="X2:"/>
            <TextBox x:Name="txtX2" Width="60" Margin="5 0" Text="7"/>
            <Label Content="h:"/>
            <TextBox x:Name="txtH" Width="60" Margin="5 0" Text="0.1"/>
            <Button x:Name="btnCalc" Content="Вычислить" Width="100" Click="btnCalc_Click" 
                    Foreground="Black" FontSize="14" FontWeight="Bold" Background="#FF78EB21"/>
        </StackPanel>

        <Image Name="imgGraph" Source="/график.jpg" Stretch="Uniform" Margin="46,25,46,25" Grid.Row="1"/>

        <DataGrid x:Name="dataGrid"
                  AutoGenerateColumns="False"
                  Grid.Row="2"
                  HeadersVisibility="Column"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="X" Binding="{Binding X}" Width="*"/>
                <DataGridTextColumn Header="Y" Binding="{Binding Y}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>